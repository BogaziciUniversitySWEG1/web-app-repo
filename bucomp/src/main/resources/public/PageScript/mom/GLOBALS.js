(function () {
    var GLOBALS = {
        UserId: null,
        CommunityId: null,
        MeetingId: null,
        Members:new Array(),
        Attendants:new Array(),
        AttendantsList:new Array()
    }
    if (!window.GLOBALS) { window.GLOBALS = GLOBALS; }
})();
