(function () {
    var GLOBALS = {
    	ServiceParameter: "api",
        topicId: "",
        tagList: new Array(),
        semTagList: new Array()
    }
    if (!window.GLOBALS) { window.GLOBALS = GLOBALS; }
})();
